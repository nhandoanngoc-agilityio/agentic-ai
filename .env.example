# LLM provider (Claude via langchain-anthropic)
ANTHROPIC_API_KEY=

# LangGraph Studio / LangSmith tracing (optional but recommended)
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=market-research-analyst-team

# Vector store / data paths
VECTORSTORE_DIR=./data/vectorstore
REPORTS_DIR=./reports

# Checkpointer (Day 10) — leave unset for local sqlite/memory checkpointing
DATABASE_URL=
